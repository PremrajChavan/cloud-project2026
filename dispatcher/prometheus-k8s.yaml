global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "dispatcher"
    static_configs:
      - targets: ["dispatcher-service:8000"]   # K8s DNS name

  - job_name: "inference_service"
    static_configs:
      - targets: ["tu-cloud-project:8001"]      # K8s DNS name